import * as menus from './menus';
import * as modals from './modals';
import * as shares from './shares';
import { default as discover } from './discover';
import { default as fakeToken } from './token';

const dataTemp = {
    menus,
    modals,
    shares,
    discover,
    fakeToken,
};

export default dataTemp;
